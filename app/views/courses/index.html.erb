<%= render "shared/form_query" %>
<h1 class="padding-left-10 small-heading"><%= "#{@courses.count} courses fitting your filters:"%></h1>
<div class="padding-left-10 make-flex">

  <div class="courses-cards">
    <%= render "shared/cards_index" %>
  </div>

  <div class="map-container">
  <div class="row card-spacing" style="margin-right: 0px; margin-left: 0px">
    <div
      id="map"
      style="width: 100%;
      height: 600px;"
      data-markers="<%= @markers.to_json %>"
    ></div>
  </div>
  </div>
</div>
